---
---

<div class="fixed top-5 right-8 z-50">
  <select id="languageSelect" class="appearance-none bg-tm-bg border border-tm-border rounded-md py-2 px-3 text-tm-text2 cursor-pointer
  focus:outline-none focus:border-tm-pr">
    <option value="en" class="bg-tm-bg text-tm-pr">🇺🇸 English</option>
    <option value="es" class="bg-tm-bg text-tm-pr">🇩🇴 Español</option>
  </select>
</div>

<style>
  #languageSelect {
    font-size: 0.9rem;
    transition: border-color 0.2s ease-in-out;
  }

  #languageSelect:hover {
    border-color: var(--color-primary);
  }

</style>

<script>
  const languageSelect = document.getElementById('languageSelect');
  if (languageSelect) {
    languageSelect.addEventListener('change', (event) => {
      const selectedLanguage = event.target.value;

      console.log('Idioma seleccionado:', selectedLanguage);

      // window.location.href = `/${selectedLanguage}`;
    });
  }
</script>
