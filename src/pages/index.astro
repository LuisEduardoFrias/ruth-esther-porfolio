---
import Layout from '@/layouts/Layout.astro'

const title = 'anslisis de datos'

const items = [
  { id: 1, texto: 'Elemento A' },
  { id: 2, texto: 'Elemento B' },
  { id: 3, texto: 'Elemento C' },
  { id: 4, texto: 'Elemento D' },
  { id: 5, texto: 'Elemento E' },
  { id: 6, texto: 'Elemento F' },
  { id: 7, texto: 'Elemento G' },
];
---

<Layout title={title}>
  <div class="content-main w-full px-2 grid grid-cols-[4rem_1fr_14rem] grid-rows-[2rem_10rem_1fr] -md:grid-cols-[1fr_10rem] gap-2">
    
    <div class="-md:hidden h-96 w-full col-start-0 col-span-1 row-start-0 row-span-3 bg-amber-800 border border-red-500">
    </div>
    
    <div class="h-full w-full col-start-2 row-start-2 -md:col-start-1 flex flex-row flex-wrap pt-5 gap-2 px-2 overflow-y-scroll border-2 border-amber-500">
      {items.map((item) => (
        <div key={item.id} class="h-10 w-full bg-amber-800 border border-red-500">
          <span>
          {item.texto}
          </span>
        </div>
      ))}
    </div>
    
    <div class="h-full w-full col-start-2 row-start-3 -md:col-start-1 bg-green-800 ">
      gghyhh
      gghyhh
      gghyhh
      gghyhh
      gghyhh
    </div>
    
    <div class="h-full w-full flex justify-center items-start p-2 col-start-3 row-start-2 row-span-2 -md:col-start-2 bg-purple-800">
      <img class="h-auto w-32 rounded-full " src="/images/profile_resize.webp" alt="profile images" />
    </imgclass>
    </div>
    
  </div>
</Layout>

<style>
  .content-main {
    height: 100vh;
    padding-bottom: 250px;
  }
</style>
